# Git Branch Workflow

最新のリモート既定ブランチから作業を始める、作業ブランチを作成・切替・確認する、といった Git ブランチ操作に使います。既定ブランチ名を決め打ちせず、ユーザーの未コミット変更を保護します。

## 前提条件

- 対象リポジトリのローカル規約を確認できること
- リモートから最新状態を取得する場合はネットワーク接続が利用できること

## プロンプト例

```text
最新のリモート既定ブランチを確認し、そこから feature/search-filter ブランチを作成してください。未コミット変更があれば変更しないでください。
```
